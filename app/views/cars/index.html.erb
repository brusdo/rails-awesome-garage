<h1>List of Cars</h1>

<ul>
  <% @cars.each do |car| %>
  <li>
    <%= car.brand %> <%= car.model %>< (Year: <%= car.year %>)
    - <%= link_to 'View Details', car_path(car) %>
  </li>
  <% end %>
</ul>
